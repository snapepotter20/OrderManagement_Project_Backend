<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8"/>
    <style>
        body {
            font-family: "Segoe UI", sans-serif;
            margin: 20px;
            font-size: 12px;
            color: #333;
        }

        header {
            text-align: center;
            margin-bottom: 20px;
        }

        h1 {
            color: #2F78C0;
        }

        .company-info {
            margin-bottom: 20px;
            text-align: center;
        }

        .summary {
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        th {
            background-color: #eeeeee;
            text-align: left;
        }

        th, td {
            padding: 8px;
            border: 1px solid #cccccc;
        }

        .footer {
            text-align: center;
            font-size: 10px;
            color: #888;
            margin-top: 30px;
        }

        .total-row {
            font-weight: bold;
        }

    </style>
</head>

<body>

<header>
    <h1>Purchase Invoice</h1>
</header>

<div class="company-info">
    <p><strong>Generated By:</strong> Procurement Management System</p>
</div>

<div class="summary">
    <p><strong>Order ID:</strong> <span th:text="${order.purchaseOrderId}"></span></p>
    <p><strong>Order Date:</strong> <span th:text="${order.orderDate}"></span></p>
    <p><strong>Expected Delivery:</strong> <span th:text="${order.expectedDelivery}"></span></p>
    <p><strong>Status:</strong> <span th:text="${order.deliveryStatus}"></span></p>
</div>

<table>
    <thead>
    <tr>
        <th>#</th>
        <th>Product ID</th>
        <th>Product Name</th>
        <th>Quantity</th>
        <th>Cost</th>
    </tr>
    </thead>
    <tbody>
    <tr th:each="item, idxStat : ${order.items}">
        <td th:text="${idxStat.index + 1}"></td>
        <td th:text="${item.product.productId}">101</td>
        <td th:text="${item.product.productName}">Widget A</td>
        <td th:text="${item.quantity}">3</td>
        <td th:text="${item.cost}">₹130.00</td>
    </tr>
    <tr class="total-row">
        <td colspan="4" style="text-align: right;">Total</td>
        <td th:text="${#numbers.formatDecimal(order.items.stream().mapToDouble(i -> i.cost).sum(), 2, 'POINT', 'COMMA')}">0.00</td>
    </tr>
    </tbody>
</table>

<div class="footer">
    <p>This invoice was generated automatically. If you have any questions, contact procurement@example.com</p>
</div>

</body>
</html>
